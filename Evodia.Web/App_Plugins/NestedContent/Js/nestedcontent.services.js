// angular.module('umbraco.filters', []).filter('htmlToPlaintext', function() {
//    return function(text) {
//        return String(text).replace(/<[^>]+>/gm, '');
//    }
//});
//
//{{ Text || mainContent | htmlToPlaintext | limitTo: 80 }}{{ mainContent.length > 80 ? "..." : "..." }}
